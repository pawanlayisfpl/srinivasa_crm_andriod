import 'package:flutter/material.dart';

class SocOrderRemarksTextField extends StatelessWidget {
  const SocOrderRemarksTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("soc order remarks textfield"),);
  }
}