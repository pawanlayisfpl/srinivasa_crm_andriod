import 'package:flutter/material.dart';

class SocProductDetailsDropDown extends StatelessWidget {
  const SocProductDetailsDropDown({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("product details dropdown"),);
  }
}