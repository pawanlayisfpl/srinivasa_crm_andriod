import 'package:flutter/material.dart';

class SocAmountDueDateField extends StatelessWidget {
  const SocAmountDueDateField({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("soc due amount date field"),);
  }
}