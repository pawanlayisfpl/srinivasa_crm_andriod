import 'package:flutter/material.dart';

class SocBalanceAmountField extends StatelessWidget {
  const SocBalanceAmountField({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("soc discount textfield"),);
  }
}